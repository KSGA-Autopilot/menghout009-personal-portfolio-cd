apiVersion: v1
kind: Service
metadata:
  name: menghout009-personal-portfolio-svc
spec:
  selector:
    app: menghout009-personal-portfolio
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
